# Phase 1.0 — Monorepo Bootstrap Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Stand up the empty-but-fully-wired HRMS monorepo: pnpm workspaces, Turborepo, NestJS api + workers, Next.js web + admin, package skeletons, Docker compose for local dev, CI pipeline, pre-commit hooks, ADR template — ending with a working `pnpm setup`, a green `GET /health` endpoint, and a green CI build.

**Architecture:** Modular monolith planned around bounded contexts (per design doc §3). This sub-plan only sets up the empty cradle — _no business logic yet_. The cradle is the discipline that everything Phase 1.1+ relies on (CI gates, pre-commit hooks, fitness functions placeholders, environment parity dev↔CI↔prod).

**Tech Stack:** pnpm 9, Turborepo 2, Node 20 LTS, TypeScript 5.5+, NestJS 10, Next.js 15, Vitest, Playwright (later), Docker Compose, GitHub Actions, husky + lint-staged + commitlint + gitleaks, Prettier, ESLint (typescript-eslint strict).

**Reference:** [`docs/superpowers/specs/2026-05-01-hrms-architecture-design.md`](../specs/2026-05-01-hrms-architecture-design.md) — sections 1-2 + Appendix H.

---

## Pre-flight checks (one-time, before Task 1)

Verify on your machine:

```bash
node --version       # must report v20.x or higher
which pnpm           # if not present: npm install -g pnpm@9
git --version        # must report 2.40+
docker --version     # Docker Desktop or Colima
git config --global user.name        # must be non-empty
git config --global user.email       # must be non-empty
```

If `git config --global user.name` returns nothing:

```bash
git config --global user.name  "Pankaj Adhikari"
git config --global user.email "pankaj.adhikari1989@gmail.com"
```

Set these once globally; never commit local repo overrides.

---

## File structure produced by this plan

```
hrms/
├── .github/
│   └── workflows/
│       └── ci.yml
├── .vscode/
│   ├── settings.json
│   └── extensions.json
├── .husky/
│   ├── pre-commit
│   └── commit-msg
├── apps/
│   ├── api/                                    # NestJS API
│   │   ├── src/
│   │   │   ├── main.ts
│   │   │   ├── app.module.ts
│   │   │   └── modules/
│   │   │       └── health/
│   │   │           ├── health.module.ts
│   │   │           └── health.controller.ts
│   │   ├── test/
│   │   │   └── health.e2e-spec.ts
│   │   ├── nest-cli.json
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   └── tsconfig.build.json
│   ├── workers/                                # NestJS standalone workers
│   │   ├── src/
│   │   │   └── main.ts
│   │   ├── package.json
│   │   └── tsconfig.json
│   ├── web/                                    # Next.js 15 customer/tenant-admin
│   │   ├── app/
│   │   │   ├── layout.tsx
│   │   │   ├── page.tsx
│   │   │   └── globals.css
│   │   ├── public/
│   │   ├── next.config.mjs
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   └── postcss.config.mjs
│   ├── admin/                                  # Next.js 15 internal admin
│   │   ├── app/
│   │   │   ├── layout.tsx
│   │   │   ├── page.tsx
│   │   │   └── globals.css
│   │   ├── next.config.mjs
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   └── postcss.config.mjs
│   └── mobile/
│       └── README.md                           # placeholder until Phase 8
├── packages/
│   ├── config/
│   │   ├── eslint/
│   │   │   ├── base.js
│   │   │   ├── nest.js
│   │   │   └── next.js
│   │   ├── typescript/
│   │   │   ├── base.json
│   │   │   ├── nest.json
│   │   │   └── next.json
│   │   ├── prettier/
│   │   │   └── index.cjs
│   │   ├── vitest/
│   │   │   └── base.ts
│   │   └── package.json
│   ├── domain/
│   │   ├── src/index.ts
│   │   ├── package.json
│   │   └── tsconfig.json
│   ├── contracts/
│   │   ├── src/index.ts
│   │   ├── package.json
│   │   └── tsconfig.json
│   ├── rules-engine/
│   │   ├── src/index.ts
│   │   ├── package.json
│   │   └── tsconfig.json
│   ├── crypto/
│   │   ├── src/index.ts
│   │   ├── package.json
│   │   └── tsconfig.json
│   ├── db/
│   │   ├── src/index.ts
│   │   ├── package.json
│   │   └── tsconfig.json
│   ├── ui/
│   │   ├── src/index.ts
│   │   ├── package.json
│   │   └── tsconfig.json
│   ├── api-client-ts/
│   │   ├── src/index.ts
│   │   ├── package.json
│   │   └── tsconfig.json
│   ├── api-client-dart/
│   │   ├── README.md
│   │   └── pubspec.yaml.placeholder
│   └── i18n/
│       ├── src/index.ts
│       ├── locales/en.json
│       ├── package.json
│       └── tsconfig.json
├── infra/
│   ├── docker/
│   │   ├── docker-compose.yml
│   │   └── otel-collector.yaml
│   └── coolify/
│       └── README.md
├── scripts/
│   ├── setup.sh
│   ├── dev-reset.sh
│   └── verify-setup.sh
├── docs/
│   ├── adr/
│   │   ├── README.md
│   │   ├── _template.md
│   │   ├── 0001-modular-monolith.md
│   │   └── 0002-postgres-with-rls.md
│   ├── dev/
│   │   ├── onboarding.md
│   │   └── troubleshooting.md
│   └── superpowers/
│       ├── specs/2026-05-01-hrms-architecture-design.md   # already exists
│       └── plans/2026-05-01-p1.0-monorepo-bootstrap.md    # this file
├── .editorconfig
├── .gitattributes
├── .gitignore
├── .gitleaks.toml
├── .node-version
├── .npmrc
├── .nvmrc
├── .prettierignore
├── commitlint.config.cjs
├── package.json
├── pnpm-workspace.yaml
├── README.md
├── tsconfig.base.json
├── tsconfig.json
└── turbo.json
```

---

## Task 1 — Git init + repo conventions

**Files:**

- Create: `/Users/pj/Documents/hrms/.gitignore`
- Create: `/Users/pj/Documents/hrms/.gitattributes`
- Create: `/Users/pj/Documents/hrms/.editorconfig`
- Create: `/Users/pj/Documents/hrms/.nvmrc`
- Create: `/Users/pj/Documents/hrms/.node-version`
- Create: `/Users/pj/Documents/hrms/.npmrc`

- [ ] **Step 1: Verify pre-flight checks (above) all pass**

If `git config --global user.name/email` is empty, set them now (see Pre-flight section).

- [ ] **Step 2: Initialize the git repository + add the GitHub remote**

```bash
cd /Users/pj/Documents/hrms
git init -b main
git remote add origin git@github.com:pankaj1989/HRMS.git
git remote -v
```

Expected output:

- `Initialized empty Git repository in /Users/pj/Documents/hrms/.git/`
- `origin  git@github.com:pankaj1989/HRMS.git (fetch)`
- `origin  git@github.com:pankaj1989/HRMS.git (push)`

(Pushing to the remote happens only at the end of P1.0 — Task 17. Intermediate commits stay local.)

- [ ] **Step 3: Write `.gitignore`**

Create `/Users/pj/Documents/hrms/.gitignore`:

```gitignore
# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
out/
.next/
.turbo/
*.tsbuildinfo

# Test outputs
coverage/
.nyc_output/
playwright-report/
test-results/

# Environment
.env
.env.local
.env.*.local
!.env.example

# IDE
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Python (for any helper scripts)
__pycache__/
*.pyc
.venv/

# Temp
tmp/
temp/

# Generated artifacts
*.pem
*.key
secrets/
.context-mode/

# Mobile (Phase 8)
apps/mobile/.dart_tool/
apps/mobile/.flutter-plugins
apps/mobile/.flutter-plugins-dependencies
apps/mobile/build/
apps/mobile/ios/Pods/
apps/mobile/ios/.symlinks/
apps/mobile/android/.gradle/

# CI artifacts kept locally
release-cert.json.local
```

- [ ] **Step 4: Write `.gitattributes`**

Create `/Users/pj/Documents/hrms/.gitattributes`:

```gitattributes
* text=auto eol=lf
*.{cmd,bat,ps1} text eol=crlf
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.tar.gz binary
*.lockb binary
package-lock.json -diff
pnpm-lock.yaml -diff
yarn.lock -diff
```

- [ ] **Step 5: Write `.editorconfig`**

Create `/Users/pj/Documents/hrms/.editorconfig`:

```editorconfig
root = true

[*]
indent_style = space
indent_size = 2
end_of_line = lf
charset = utf-8
trim_trailing_whitespace = true
insert_final_newline = true

[*.{md,markdown}]
trim_trailing_whitespace = false

[Makefile]
indent_style = tab

[*.{yml,yaml}]
indent_size = 2
```

- [ ] **Step 6: Pin Node version**

Create `/Users/pj/Documents/hrms/.nvmrc`:

```
20.18.0
```

Create `/Users/pj/Documents/hrms/.node-version`:

```
20.18.0
```

(Two files because `nvm` reads `.nvmrc`, `fnm`/`mise`/`asdf` read `.node-version`. Both 2-line files prevent any "wrong Node version" debates.)

- [ ] **Step 7: Configure pnpm via `.npmrc`**

Create `/Users/pj/Documents/hrms/.npmrc`:

```
auto-install-peers=true
strict-peer-dependencies=false
prefer-workspace-packages=true
link-workspace-packages=true
shared-workspace-lockfile=true
save-exact=true
```

- [ ] **Step 8: Stage everything currently in the repo**

```bash
cd /Users/pj/Documents/hrms
git add .gitignore .gitattributes .editorconfig .nvmrc .node-version .npmrc spec/ doc-zip/ docs/
git status
```

Expected: a long list of untracked files now staged. `.DS_Store` is correctly excluded by `.gitignore`.

- [ ] **Step 9: Initial commit**

```bash
git commit -m "$(cat <<'EOF'
chore: initial commit — spec, design doc, repo conventions

- spec/ — 10-module HRMS functional spec (foundation through analytics)
- doc-zip/ — bundled spec
- docs/superpowers/specs/2026-05-01-hrms-architecture-design.md — approved architecture
- docs/superpowers/plans/2026-05-01-p1.0-monorepo-bootstrap.md — this plan
- .gitignore, .gitattributes, .editorconfig, .nvmrc, .node-version, .npmrcEOF
)"
git log --oneline
```

Expected: one commit on `main`.

- [ ] **Step 10: Tag the empty cradle for traceability**

```bash
git tag -a p1.0-start -m "Phase 1.0 monorepo bootstrap — start"
git tag --list
```

Expected output: `p1.0-start`.

---

## Task 2 — pnpm workspace + Turborepo

**Files:**

- Create: `package.json`
- Create: `pnpm-workspace.yaml`
- Create: `turbo.json`

- [ ] **Step 1: Install pnpm globally if missing**

```bash
which pnpm || npm install -g pnpm@9
pnpm --version
```

Expected: `9.x.x`.

- [ ] **Step 2: Write root `package.json`**

Create `/Users/pj/Documents/hrms/package.json`:

```json
{
  "name": "hrms",
  "version": "0.0.0",
  "private": true,
  "description": "HRMS — Indian compliance-first multi-tenant HR platform",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20.18.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,yml,yaml}\" --ignore-path .prettierignore",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md,yml,yaml}\" --ignore-path .prettierignore",
    "type": "turbo run type",
    "test": "turbo run test",
    "test:unit": "turbo run test:unit",
    "test:e2e": "turbo run test:e2e",
    "clean": "turbo run clean && rm -rf node_modules",
    "setup": "bash scripts/setup.sh",
    "verify": "bash scripts/verify-setup.sh",
    "dev:reset": "bash scripts/dev-reset.sh",
    "docker:up": "docker compose -f infra/docker/docker-compose.yml up -d",
    "docker:down": "docker compose -f infra/docker/docker-compose.yml down",
    "docker:logs": "docker compose -f infra/docker/docker-compose.yml logs -f",
    "prepare": "husky"
  },
  "devDependencies": {
    "turbo": "2.1.3",
    "typescript": "5.6.2",
    "prettier": "3.3.3",
    "husky": "9.1.6",
    "lint-staged": "15.2.10",
    "@commitlint/cli": "19.5.0",
    "@commitlint/config-conventional": "19.5.0"
  }
}
```

- [ ] **Step 3: Write `pnpm-workspace.yaml`**

Create `/Users/pj/Documents/hrms/pnpm-workspace.yaml`:

```yaml
packages:
  - 'apps/*'
  - 'packages/*'
```

- [ ] **Step 4: Write `turbo.json`**

Create `/Users/pj/Documents/hrms/turbo.json`:

```json
{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**", "build/**"],
      "inputs": [
        "src/**",
        "app/**",
        "tsconfig*.json",
        "package.json",
        "next.config.*",
        "nest-cli.json"
      ]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "outputs": []
    },
    "type": {
      "dependsOn": ["^build"],
      "outputs": ["*.tsbuildinfo"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"],
      "inputs": ["src/**", "test/**", "tests/**", "vitest.config.*", "package.json"]
    },
    "test:unit": {
      "outputs": ["coverage/**"]
    },
    "test:e2e": {
      "dependsOn": ["build"],
      "outputs": ["playwright-report/**", "test-results/**"]
    },
    "clean": {
      "cache": false
    }
  }
}
```

- [ ] **Step 5: Install dependencies**

```bash
cd /Users/pj/Documents/hrms
pnpm install
```

Expected: lockfile generated; node_modules installed; ~30s on first run.

- [ ] **Step 6: Verify Turborepo works**

```bash
pnpm turbo --version
```

Expected: `2.1.x`.

- [ ] **Step 7: Commit**

```bash
git add package.json pnpm-workspace.yaml turbo.json pnpm-lock.yaml
git commit -m "$(cat <<'EOF'
chore: add pnpm workspace + turborepo

Pinned Node 20.18.0, pnpm 9.12.0. Turbo tasks: build/dev/lint/type/test
with input/output declarations for caching.EOF
)"
```

---

## Task 3 — TypeScript shared config

**Files:**

- Create: `tsconfig.base.json`
- Create: `tsconfig.json`
- Create: `packages/config/package.json`
- Create: `packages/config/typescript/base.json`
- Create: `packages/config/typescript/nest.json`
- Create: `packages/config/typescript/next.json`

- [ ] **Step 1: Write `tsconfig.base.json` (root)**

Create `/Users/pj/Documents/hrms/tsconfig.base.json`:

```json
{
  "$schema": "https://json.schemastore.org/tsconfig",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "verbatimModuleSyntax": false,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "noImplicitAny": true,
    "strictNullChecks": true,
    "strictFunctionTypes": true,
    "strictBindCallApply": true,
    "strictPropertyInitialization": true,
    "noImplicitThis": true,
    "useUnknownInCatchVariables": true,
    "alwaysStrict": true,
    "exactOptionalPropertyTypes": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "noPropertyAccessFromIndexSignature": false,
    "noFallthroughCasesInSwitch": true,
    "noImplicitReturns": true,
    "noUnusedLocals": false,
    "noUnusedParameters": false,
    "incremental": true,
    "composite": false,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "removeComments": false,
    "preserveConstEnums": true
  },
  "exclude": ["node_modules", "dist", ".next", "build", "coverage"]
}
```

- [ ] **Step 2: Write root `tsconfig.json`**

Create `/Users/pj/Documents/hrms/tsconfig.json`:

```json
{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["scripts/**/*.ts", "*.config.ts", "*.config.cts", "*.config.mts"]
}
```

- [ ] **Step 3: Create `packages/config` workspace package**

Create `/Users/pj/Documents/hrms/packages/config/package.json`:

```json
{
  "name": "@hrms/config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared lint/format/test/ts configs",
  "license": "UNLICENSED",
  "main": "./index.js",
  "files": ["eslint", "typescript", "prettier", "vitest"]
}
```

- [ ] **Step 4: Write the three TypeScript variants**

Create `/Users/pj/Documents/hrms/packages/config/typescript/base.json`:

```json
{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "${configDir}/dist",
    "rootDir": "${configDir}/src",
    "tsBuildInfoFile": "${configDir}/dist/.tsbuildinfo"
  },
  "include": ["${configDir}/src/**/*.ts"],
  "exclude": ["${configDir}/node_modules", "${configDir}/dist"]
}
```

Create `/Users/pj/Documents/hrms/packages/config/typescript/nest.json`:

```json
{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./base.json",
  "compilerOptions": {
    "module": "CommonJS",
    "moduleResolution": "Node",
    "target": "ES2022",
    "lib": ["ES2022"],
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "strictPropertyInitialization": false,
    "outDir": "${configDir}/dist",
    "baseUrl": "${configDir}",
    "incremental": true,
    "skipLibCheck": true
  }
}
```

Create `/Users/pj/Documents/hrms/packages/config/typescript/next.json`:

```json
{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["dom", "dom.iterable", "esnext"],
    "jsx": "preserve",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "noEmit": true,
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "allowJs": true,
    "resolveJsonModule": true,
    "isolatedModules": true
  }
}
```

- [ ] **Step 5: Commit**

```bash
git add tsconfig.base.json tsconfig.json packages/config/
git commit -m "$(cat <<'EOF'
chore: add shared TypeScript configs

Strict mode + noUncheckedIndexedAccess + exactOptionalPropertyTypes baked in.
Three variants: base (libs/scripts), nest (decorators+CJS), next (jsx+bundler).EOF
)"
```

---

## Task 4 — Prettier + ESLint + commitlint + gitleaks

**Files:**

- Create: `packages/config/prettier/index.cjs`
- Create: `.prettierignore`
- Create: `packages/config/eslint/base.js`
- Create: `packages/config/eslint/nest.js`
- Create: `packages/config/eslint/next.js`
- Create: `commitlint.config.cjs`
- Create: `.gitleaks.toml`

- [ ] **Step 1: Prettier config**

Create `/Users/pj/Documents/hrms/packages/config/prettier/index.cjs`:

```js
/** @type {import("prettier").Config} */
module.exports = {
  printWidth: 100,
  tabWidth: 2,
  useTabs: false,
  semi: true,
  singleQuote: true,
  quoteProps: 'as-needed',
  trailingComma: 'all',
  bracketSpacing: true,
  bracketSameLine: false,
  arrowParens: 'always',
  endOfLine: 'lf',
  embeddedLanguageFormatting: 'auto',
  plugins: [],
};
```

Add to root `package.json` `"prettier"` field by appending it (we already have prettier in devDeps; add config reference). Edit `/Users/pj/Documents/hrms/package.json`:

Add this line at the top level (after the `"packageManager"` field):

```json
  "prettier": "@hrms/config/prettier",
```

(So the final `package.json` has the `"prettier"` key alongside `"scripts"`, `"devDependencies"` etc.)

- [ ] **Step 2: Prettier ignore**

Create `/Users/pj/Documents/hrms/.prettierignore`:

```gitignore
node_modules/
dist/
.next/
.turbo/
build/
coverage/
.pnpm-store/
pnpm-lock.yaml
package-lock.json
yarn.lock
*.min.js
*.min.css
spec/
doc-zip/
```

- [ ] **Step 3: Install Prettier dependency in `@hrms/config`**

```bash
cd /Users/pj/Documents/hrms
pnpm add -w -D prettier@3.3.3
pnpm add --filter @hrms/config prettier@3.3.3
```

- [ ] **Step 4: Verify Prettier works**

```bash
pnpm format:check
```

Expected: passes (no files yet to fix). If errors, run `pnpm format` and inspect.

- [ ] **Step 5: ESLint base config**

Create `/Users/pj/Documents/hrms/packages/config/eslint/base.js`:

```js
const tseslint = require('typescript-eslint');
const eslint = require('@eslint/js');

/** @type {import('eslint').Linter.Config[]} */
module.exports = [
  eslint.configs.recommended,
  ...tseslint.configs.strictTypeChecked,
  ...tseslint.configs.stylisticTypeChecked,
  {
    languageOptions: {
      parserOptions: {
        projectService: true,
      },
    },
    rules: {
      '@typescript-eslint/no-unused-vars': [
        'warn',
        { argsIgnorePattern: '^_', varsIgnorePattern: '^_' },
      ],
      '@typescript-eslint/consistent-type-imports': 'error',
      '@typescript-eslint/no-import-type-side-effects': 'error',
      '@typescript-eslint/no-explicit-any': 'error',
      '@typescript-eslint/no-non-null-assertion': 'error',
      'no-console': ['warn', { allow: ['warn', 'error'] }],
      eqeqeq: ['error', 'always'],
      'no-implicit-coercion': 'error',
      'no-restricted-syntax': [
        'error',
        {
          selector: "CallExpression[callee.name='eval']",
          message: 'eval() is forbidden — even in tests.',
        },
      ],
    },
  },
  {
    ignores: ['**/dist/**', '**/.next/**', '**/node_modules/**', '**/coverage/**'],
  },
];
```

- [ ] **Step 6: ESLint Nest variant**

Create `/Users/pj/Documents/hrms/packages/config/eslint/nest.js`:

```js
const base = require('./base');

module.exports = [
  ...base,
  {
    rules: {
      '@typescript-eslint/explicit-function-return-type': 'off',
      '@typescript-eslint/explicit-module-boundary-types': 'off',
      '@typescript-eslint/no-extraneous-class': 'off',
    },
  },
];
```

- [ ] **Step 7: ESLint Next variant**

Create `/Users/pj/Documents/hrms/packages/config/eslint/next.js`:

```js
const base = require('./base');

module.exports = [
  ...base,
  {
    files: ['**/*.tsx'],
    rules: {
      'react/react-in-jsx-scope': 'off',
    },
  },
];
```

- [ ] **Step 8: Install ESLint + typescript-eslint**

```bash
pnpm add -w -D eslint@9.12.0 typescript-eslint@8.8.0 @eslint/js@9.12.0
pnpm add --filter @hrms/config eslint@9.12.0 typescript-eslint@8.8.0 @eslint/js@9.12.0
```

- [ ] **Step 9: commitlint config**

Create `/Users/pj/Documents/hrms/commitlint.config.cjs`:

```js
/** @type {import('@commitlint/types').UserConfig} */
module.exports = {
  extends: ['@commitlint/config-conventional'],
  rules: {
    'type-enum': [
      2,
      'always',
      [
        'feat',
        'fix',
        'docs',
        'style',
        'refactor',
        'perf',
        'test',
        'build',
        'ci',
        'chore',
        'revert',
        'security',
      ],
    ],
    'subject-case': [2, 'never', ['upper-case', 'pascal-case', 'start-case']],
    'header-max-length': [2, 'always', 100],
    'body-max-line-length': [2, 'always', 100],
  },
};
```

- [ ] **Step 10: gitleaks config**

Create `/Users/pj/Documents/hrms/.gitleaks.toml`:

```toml
title = "HRMS gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Files we trust to contain pseudo-secrets (test fixtures, samples)"
paths = [
  '''(?i).*\.example\.(env|json)$''',
  '''(?i)docs/.*''',
  '''(?i)spec/.*''',
]
```

- [ ] **Step 11: Verify everything resolves**

```bash
pnpm install
pnpm format:check
```

Expected: both succeed.

- [ ] **Step 12: Commit**

```bash
git add packages/config/ .prettierignore commitlint.config.cjs .gitleaks.toml package.json pnpm-lock.yaml
git commit -m "$(cat <<'EOF'
chore: add Prettier, ESLint (typescript-eslint strict), commitlint, gitleaks

ESLint runs typescript-eslint strictTypeChecked + stylisticTypeChecked.
Three variants in @hrms/config: base, nest, next. Conventional commit
types enforced; secret scanning rules in .gitleaks.toml.EOF
)"
```

---

## Task 5 — Husky + lint-staged + pre-commit hooks

**Files:**

- Create: `.husky/pre-commit`
- Create: `.husky/commit-msg`
- Modify: `package.json` (add `lint-staged` config)

- [ ] **Step 1: Initialize husky**

```bash
cd /Users/pj/Documents/hrms
pnpm exec husky init
```

This creates `.husky/pre-commit` automatically. Replace its contents below.

- [ ] **Step 2: Write `.husky/pre-commit`**

Overwrite `/Users/pj/Documents/hrms/.husky/pre-commit`:

```bash
#!/usr/bin/env sh
# Run lint-staged (format + lint changed files)
pnpm exec lint-staged

# Block commits with secrets — gitleaks must be installed locally
if command -v gitleaks > /dev/null 2>&1; then
  gitleaks protect --staged --redact --config=.gitleaks.toml || {
    echo "✗ gitleaks found secrets in staged changes. Aborting commit."
    exit 1
  }
else
  echo "⚠ gitleaks not installed locally; skipping secret scan."
  echo "  Install: brew install gitleaks  (macOS)  |  https://github.com/gitleaks/gitleaks (other)"
fi
```

```bash
chmod +x .husky/pre-commit
```

- [ ] **Step 3: Write `.husky/commit-msg`**

Create `/Users/pj/Documents/hrms/.husky/commit-msg`:

```bash
#!/usr/bin/env sh
pnpm exec commitlint --edit "$1"
```

```bash
chmod +x .husky/commit-msg
```

- [ ] **Step 4: Configure `lint-staged` in root `package.json`**

Add to `/Users/pj/Documents/hrms/package.json` (top level, alongside other fields):

```json
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": [
      "prettier --write"
    ],
    "*.{json,md,yml,yaml}": [
      "prettier --write"
    ]
  },
```

- [ ] **Step 5: Verify hooks are registered**

```bash
ls -la .husky
git config core.hooksPath
```

Expected: `.husky/pre-commit`, `.husky/commit-msg` listed; `core.hooksPath` returns `.husky` (set by `husky init`).

- [ ] **Step 6: Test the pre-commit hook**

Touch a JS file and stage it:

```bash
echo "const   x = 1;" > /tmp/test.ts && cp /tmp/test.ts ./_test_hook.ts
git add ./_test_hook.ts
git commit -m "test: hook test"
```

Expected: file is auto-formatted (multiple spaces → single space) by lint-staged before the commit succeeds.

Cleanup:

```bash
git reset HEAD~1 --hard
rm -f _test_hook.ts
```

- [ ] **Step 7: Test commitlint blocks malformed messages**

```bash
git commit --allow-empty -m "this is bad"
```

Expected: commit fails with `subject may not be empty` or `type must be one of [...]`.

```bash
git commit --allow-empty -m "chore: this is fine"
```

Expected: commit succeeds. Then revert it:

```bash
git reset HEAD~1 --hard
```

- [ ] **Step 8: Commit hook configuration**

```bash
git add .husky/ package.json
git commit -m "$(cat <<'EOF'
chore: add husky + lint-staged + commitlint pre-commit hooks

pre-commit: runs lint-staged (Prettier on changed files) + gitleaks scan
commit-msg: enforces conventional commit formatEOF
)"
```

---

## Task 6 — NestJS API skeleton with health endpoint (TDD)

**Files:**

- Create: `apps/api/package.json`
- Create: `apps/api/tsconfig.json`
- Create: `apps/api/tsconfig.build.json`
- Create: `apps/api/nest-cli.json`
- Create: `apps/api/src/main.ts`
- Create: `apps/api/src/app.module.ts`
- Create: `apps/api/src/modules/health/health.module.ts`
- Create: `apps/api/src/modules/health/health.controller.ts`
- Create: `apps/api/test/health.e2e-spec.ts`
- Create: `apps/api/test/jest-e2e.json`

- [ ] **Step 1: Create `apps/api/package.json`**

Create `/Users/pj/Documents/hrms/apps/api/package.json`:

```json
{
  "name": "@hrms/api",
  "version": "0.0.0",
  "private": true,
  "description": "HRMS NestJS API",
  "license": "UNLICENSED",
  "scripts": {
    "build": "nest build",
    "dev": "nest start --watch",
    "start": "node dist/main.js",
    "lint": "eslint src test --max-warnings 0",
    "type": "tsc --noEmit",
    "test": "jest --config ./test/jest-e2e.json",
    "test:unit": "jest --passWithNoTests",
    "test:e2e": "jest --config ./test/jest-e2e.json",
    "test:watch": "jest --watch",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@nestjs/common": "10.4.4",
    "@nestjs/core": "10.4.4",
    "@nestjs/platform-express": "10.4.4",
    "reflect-metadata": "0.2.2",
    "rxjs": "7.8.1"
  },
  "devDependencies": {
    "@hrms/config": "workspace:*",
    "@nestjs/cli": "10.4.5",
    "@nestjs/schematics": "10.1.4",
    "@nestjs/testing": "10.4.4",
    "@types/express": "5.0.0",
    "@types/jest": "29.5.13",
    "@types/node": "22.7.4",
    "@types/supertest": "6.0.2",
    "jest": "29.7.0",
    "supertest": "7.0.0",
    "ts-jest": "29.2.5",
    "ts-loader": "9.5.1",
    "tsconfig-paths": "4.2.0",
    "typescript": "5.6.2"
  }
}
```

- [ ] **Step 2: TS configs for the api**

Create `/Users/pj/Documents/hrms/apps/api/tsconfig.json`:

```json
{
  "extends": "@hrms/config/typescript/nest.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "baseUrl": "./",
    "paths": {
      "@app/*": ["src/*"]
    }
  },
  "include": ["src/**/*", "test/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

Create `/Users/pj/Documents/hrms/apps/api/tsconfig.build.json`:

```json
{
  "extends": "./tsconfig.json",
  "exclude": ["node_modules", "test", "dist", "**/*.spec.ts", "**/*.e2e-spec.ts"]
}
```

Create `/Users/pj/Documents/hrms/apps/api/nest-cli.json`:

```json
{
  "$schema": "https://json.schemastore.org/nest-cli",
  "collection": "@nestjs/schematics",
  "sourceRoot": "src",
  "compilerOptions": {
    "deleteOutDir": true
  }
}
```

- [ ] **Step 3: Write the failing e2e test FIRST**

Create `/Users/pj/Documents/hrms/apps/api/test/jest-e2e.json`:

```json
{
  "moduleFileExtensions": ["js", "json", "ts"],
  "rootDir": ".",
  "testEnvironment": "node",
  "testRegex": ".e2e-spec.ts$",
  "transform": {
    "^.+\\.(t|j)s$": "ts-jest"
  }
}
```

Create `/Users/pj/Documents/hrms/apps/api/test/health.e2e-spec.ts`:

```typescript
import { Test, TestingModule } from '@nestjs/testing';
import { INestApplication } from '@nestjs/common';
import request from 'supertest';
import { AppModule } from '../src/app.module';

describe('Health (e2e)', () => {
  let app: INestApplication;

  beforeAll(async () => {
    const moduleFixture: TestingModule = await Test.createTestingModule({
      imports: [AppModule],
    }).compile();

    app = moduleFixture.createNestApplication();
    await app.init();
  });

  afterAll(async () => {
    await app.close();
  });

  it('GET /health returns 200 with shape { status, uptime, version, timestamp }', async () => {
    const response = await request(app.getHttpServer()).get('/health');

    expect(response.status).toBe(200);
    expect(response.body).toEqual(
      expect.objectContaining({
        status: 'ok',
        uptime: expect.any(Number),
        version: expect.any(String),
        timestamp: expect.any(String),
      }),
    );
    expect(new Date(response.body.timestamp).toString()).not.toBe('Invalid Date');
    expect(response.body.uptime).toBeGreaterThanOrEqual(0);
  });

  it('GET /health/liveness returns 200 with { status: ok }', async () => {
    const response = await request(app.getHttpServer()).get('/health/liveness');
    expect(response.status).toBe(200);
    expect(response.body).toEqual({ status: 'ok' });
  });
});
```

- [ ] **Step 4: Install API deps and run the test — expect it to FAIL**

```bash
cd /Users/pj/Documents/hrms
pnpm install
cd apps/api
pnpm test:e2e
```

Expected: FAIL — module `'../src/app.module'` cannot be resolved (we haven't written it yet). This is the red phase of TDD.

- [ ] **Step 5: Write the minimal NestJS app to make the test pass**

Create `/Users/pj/Documents/hrms/apps/api/src/main.ts`:

```typescript
import 'reflect-metadata';
import { NestFactory } from '@nestjs/core';
import { Logger, ShutdownSignal } from '@nestjs/common';
import { AppModule } from './app.module';

async function bootstrap(): Promise<void> {
  const app = await NestFactory.create(AppModule, {
    bufferLogs: true,
    abortOnError: false,
  });

  app.enableShutdownHooks([ShutdownSignal.SIGTERM, ShutdownSignal.SIGINT]);

  const port = Number(process.env.PORT ?? 3001);
  await app.listen(port);

  const logger = new Logger('Bootstrap');
  logger.log(`HRMS API listening on http://localhost:${port}`);
}

void bootstrap();
```

Create `/Users/pj/Documents/hrms/apps/api/src/app.module.ts`:

```typescript
import { Module } from '@nestjs/common';
import { HealthModule } from './modules/health/health.module';

@Module({
  imports: [HealthModule],
})
export class AppModule {}
```

Create `/Users/pj/Documents/hrms/apps/api/src/modules/health/health.module.ts`:

```typescript
import { Module } from '@nestjs/common';
import { HealthController } from './health.controller';

@Module({
  controllers: [HealthController],
})
export class HealthModule {}
```

Create `/Users/pj/Documents/hrms/apps/api/src/modules/health/health.controller.ts`:

```typescript
import { Controller, Get, HttpCode, HttpStatus } from '@nestjs/common';

const SERVICE_VERSION = process.env.SERVICE_VERSION ?? '0.0.0-dev';

@Controller('health')
export class HealthController {
  @Get()
  @HttpCode(HttpStatus.OK)
  getHealth(): {
    status: 'ok';
    uptime: number;
    version: string;
    timestamp: string;
  } {
    return {
      status: 'ok',
      uptime: process.uptime(),
      version: SERVICE_VERSION,
      timestamp: new Date().toISOString(),
    };
  }

  @Get('liveness')
  @HttpCode(HttpStatus.OK)
  getLiveness(): { status: 'ok' } {
    return { status: 'ok' };
  }
}
```

- [ ] **Step 6: Run the test again — expect PASS**

```bash
cd /Users/pj/Documents/hrms/apps/api
pnpm test:e2e
```

Expected: 2 specs pass.

- [ ] **Step 7: Verify dev server boots**

```bash
pnpm dev
```

In a second terminal:

```bash
curl -i http://localhost:3001/health
curl -i http://localhost:3001/health/liveness
```

Expected: both return 200 with the documented JSON shape. Stop dev server with Ctrl+C.

- [ ] **Step 8: Verify build succeeds**

```bash
pnpm build
ls dist
```

Expected: `dist/main.js` exists.

- [ ] **Step 9: Commit**

```bash
cd /Users/pj/Documents/hrms
git add apps/api/ pnpm-lock.yaml
git commit -m "$(cat <<'EOF'
feat(api): NestJS skeleton with health endpoint

- GET /health returns { status, uptime, version, timestamp }
- GET /health/liveness for k8s/Coolify probe
- TDD: e2e test written first, validated red→green
- Strict TS, ts-jest, supertestEOF
)"
```

---

## Task 7 — NestJS workers skeleton

**Files:**

- Create: `apps/workers/package.json`
- Create: `apps/workers/tsconfig.json`
- Create: `apps/workers/src/main.ts`

- [ ] **Step 1: package.json**

Create `/Users/pj/Documents/hrms/apps/workers/package.json`:

```json
{
  "name": "@hrms/workers",
  "version": "0.0.0",
  "private": true,
  "description": "HRMS BullMQ workers process — placeholder until Phase 1.3",
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "lint": "eslint src --max-warnings 0",
    "type": "tsc --noEmit",
    "test": "echo 'no tests yet — added in Phase 1.3' && exit 0",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "reflect-metadata": "0.2.2"
  },
  "devDependencies": {
    "@hrms/config": "workspace:*",
    "@types/node": "22.7.4",
    "tsx": "4.19.1",
    "typescript": "5.6.2"
  }
}
```

- [ ] **Step 2: tsconfig**

Create `/Users/pj/Documents/hrms/apps/workers/tsconfig.json`:

```json
{
  "extends": "@hrms/config/typescript/nest.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"]
}
```

- [ ] **Step 3: Minimal `main.ts` with shutdown hooks**

Create `/Users/pj/Documents/hrms/apps/workers/src/main.ts`:

```typescript
import 'reflect-metadata';

const SHUTDOWN_SIGNALS = ['SIGINT', 'SIGTERM'] as const;

async function start(): Promise<void> {
  // Phase 1.3 will wire BullMQ workers here.
  // For Phase 1.0, this is a long-running process that simply waits for shutdown.
  console.log('[workers] HRMS workers process started — awaiting Phase 1.3 wiring.');

  await new Promise<void>((resolve) => {
    SHUTDOWN_SIGNALS.forEach((signal) => {
      process.once(signal, () => {
        console.log(`[workers] received ${signal} — shutting down.`);
        resolve();
      });
    });
  });

  console.log('[workers] graceful shutdown complete.');
  process.exit(0);
}

void start();
```

- [ ] **Step 4: Install + build**

```bash
cd /Users/pj/Documents/hrms
pnpm install
cd apps/workers
pnpm build
ls dist
```

Expected: `dist/main.js` exists.

- [ ] **Step 5: Verify worker starts and shuts down**

```bash
cd /Users/pj/Documents/hrms/apps/workers
node dist/main.js &
WORKER_PID=$!
sleep 1
kill -TERM $WORKER_PID
wait $WORKER_PID
```

Expected output includes "started", "received SIGTERM", "graceful shutdown complete".

- [ ] **Step 6: Commit**

```bash
cd /Users/pj/Documents/hrms
git add apps/workers/ pnpm-lock.yaml
git commit -m "$(cat <<'EOF'
feat(workers): NestJS workers process skeleton

Long-running process with SIGINT/SIGTERM graceful shutdown.
BullMQ wiring deferred to Phase 1.3.EOF
)"
```

---

## Task 8 — Next.js 15 web app skeleton

**Files:**

- Create: `apps/web/package.json`
- Create: `apps/web/tsconfig.json`
- Create: `apps/web/next.config.mjs`
- Create: `apps/web/postcss.config.mjs`
- Create: `apps/web/app/layout.tsx`
- Create: `apps/web/app/page.tsx`
- Create: `apps/web/app/globals.css`

- [ ] **Step 1: package.json**

Create `/Users/pj/Documents/hrms/apps/web/package.json`:

```json
{
  "name": "@hrms/web",
  "version": "0.0.0",
  "private": true,
  "description": "HRMS customer + tenant-admin web (Next.js 15)",
  "license": "UNLICENSED",
  "scripts": {
    "build": "next build",
    "dev": "next dev --port 3000",
    "start": "next start --port 3000",
    "lint": "next lint --max-warnings 0",
    "type": "tsc --noEmit",
    "test": "echo 'unit tests added Phase 1.6' && exit 0",
    "clean": "rm -rf .next .turbo"
  },
  "dependencies": {
    "next": "15.0.0",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@hrms/config": "workspace:*",
    "@types/node": "22.7.4",
    "@types/react": "18.3.11",
    "@types/react-dom": "18.3.0",
    "eslint-config-next": "15.0.0",
    "typescript": "5.6.2"
  }
}
```

- [ ] **Step 2: tsconfig**

Create `/Users/pj/Documents/hrms/apps/web/tsconfig.json`:

```json
{
  "extends": "@hrms/config/typescript/next.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

- [ ] **Step 3: next.config.mjs**

Create `/Users/pj/Documents/hrms/apps/web/next.config.mjs`:

```js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  poweredByHeader: false,
  output: 'standalone',
  experimental: {
    typedRoutes: true,
  },
};

export default nextConfig;
```

- [ ] **Step 4: postcss config**

Create `/Users/pj/Documents/hrms/apps/web/postcss.config.mjs`:

```js
export default {
  plugins: {},
};
```

- [ ] **Step 5: App router files**

Create `/Users/pj/Documents/hrms/apps/web/app/layout.tsx`:

```typescript
import type { Metadata } from 'next';
import type { ReactNode } from 'react';
import './globals.css';

export const metadata: Metadata = {
  title: 'HRMS',
  description: 'Indian compliance-first multi-tenant HR platform',
};

export default function RootLayout({
  children,
}: {
  children: ReactNode;
}): JSX.Element {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
```

Create `/Users/pj/Documents/hrms/apps/web/app/page.tsx`:

```typescript
export default function HomePage(): JSX.Element {
  return (
    <main style={{ padding: '2rem', fontFamily: 'system-ui, sans-serif' }}>
      <h1>HRMS</h1>
      <p>
        Phase 1.0 monorepo bootstrap is live. Web shell will be filled out in
        Phase 7.
      </p>
      <ul>
        <li>
          API health: <a href="http://localhost:3001/health">localhost:3001/health</a>
        </li>
        <li>
          Admin: <a href="http://localhost:3002">localhost:3002</a>
        </li>
      </ul>
    </main>
  );
}
```

Create `/Users/pj/Documents/hrms/apps/web/app/globals.css`:

```css
:root {
  color-scheme: light dark;
  --bg: #ffffff;
  --fg: #0a0a0a;
}

@media (prefers-color-scheme: dark) {
  :root {
    --bg: #0a0a0a;
    --fg: #ededed;
  }
}

* {
  box-sizing: border-box;
}

html,
body {
  margin: 0;
  padding: 0;
  background: var(--bg);
  color: var(--fg);
}
```

- [ ] **Step 6: Install + build**

```bash
cd /Users/pj/Documents/hrms
pnpm install
cd apps/web
pnpm build
```

Expected: `.next` directory built; no errors.

- [ ] **Step 7: Verify dev server starts**

```bash
pnpm dev
```

In another terminal:

```bash
curl -i http://localhost:3000
```

Expected: HTML body containing "HRMS". Stop with Ctrl+C.

- [ ] **Step 8: Commit**

```bash
cd /Users/pj/Documents/hrms
git add apps/web/ pnpm-lock.yaml
git commit -m "$(cat <<'EOF'
feat(web): Next.js 15 customer/tenant-admin shell

App Router, strict TS, standalone output for Coolify deploy.
Landing page links to API health + admin app for sanity-check.EOF
)"
```

---

## Task 9 — Next.js 15 admin app skeleton

**Files:**

- Create: `apps/admin/package.json`
- Create: `apps/admin/tsconfig.json`
- Create: `apps/admin/next.config.mjs`
- Create: `apps/admin/postcss.config.mjs`
- Create: `apps/admin/app/layout.tsx`
- Create: `apps/admin/app/page.tsx`
- Create: `apps/admin/app/globals.css`

- [ ] **Step 1: package.json**

Create `/Users/pj/Documents/hrms/apps/admin/package.json` (mirror of web with port 3002):

```json
{
  "name": "@hrms/admin",
  "version": "0.0.0",
  "private": true,
  "description": "HRMS internal-platform admin (Next.js 15)",
  "license": "UNLICENSED",
  "scripts": {
    "build": "next build",
    "dev": "next dev --port 3002",
    "start": "next start --port 3002",
    "lint": "next lint --max-warnings 0",
    "type": "tsc --noEmit",
    "test": "echo 'unit tests added Phase 1.6' && exit 0",
    "clean": "rm -rf .next .turbo"
  },
  "dependencies": {
    "next": "15.0.0",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@hrms/config": "workspace:*",
    "@types/node": "22.7.4",
    "@types/react": "18.3.11",
    "@types/react-dom": "18.3.0",
    "eslint-config-next": "15.0.0",
    "typescript": "5.6.2"
  }
}
```

- [ ] **Step 2: tsconfig (identical to web)**

Create `/Users/pj/Documents/hrms/apps/admin/tsconfig.json`:

```json
{
  "extends": "@hrms/config/typescript/next.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

- [ ] **Step 3: next.config.mjs (identical)**

Create `/Users/pj/Documents/hrms/apps/admin/next.config.mjs`:

```js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  poweredByHeader: false,
  output: 'standalone',
  experimental: {
    typedRoutes: true,
  },
};

export default nextConfig;
```

- [ ] **Step 4: postcss config (identical)**

Create `/Users/pj/Documents/hrms/apps/admin/postcss.config.mjs`:

```js
export default {
  plugins: {},
};
```

- [ ] **Step 5: App router files**

Create `/Users/pj/Documents/hrms/apps/admin/app/layout.tsx`:

```typescript
import type { Metadata } from 'next';
import type { ReactNode } from 'react';
import './globals.css';

export const metadata: Metadata = {
  title: 'HRMS Admin',
  description: 'Internal platform administration',
  robots: { index: false, follow: false },
};

export default function RootLayout({
  children,
}: {
  children: ReactNode;
}): JSX.Element {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
```

Create `/Users/pj/Documents/hrms/apps/admin/app/page.tsx`:

```typescript
export default function AdminHome(): JSX.Element {
  return (
    <main style={{ padding: '2rem', fontFamily: 'system-ui, sans-serif' }}>
      <h1>HRMS Admin</h1>
      <p>
        Internal platform admin shell. Auth + RBAC arrives in Phase 1.2.
      </p>
    </main>
  );
}
```

Create `/Users/pj/Documents/hrms/apps/admin/app/globals.css` (identical to web's):

```css
:root {
  color-scheme: light dark;
  --bg: #ffffff;
  --fg: #0a0a0a;
}

@media (prefers-color-scheme: dark) {
  :root {
    --bg: #0a0a0a;
    --fg: #ededed;
  }
}

* {
  box-sizing: border-box;
}

html,
body {
  margin: 0;
  padding: 0;
  background: var(--bg);
  color: var(--fg);
}
```

- [ ] **Step 6: Install + build + verify**

```bash
cd /Users/pj/Documents/hrms
pnpm install
cd apps/admin
pnpm build
pnpm dev &
sleep 5
curl -i http://localhost:3002
kill %1
```

Expected: HTML containing "HRMS Admin".

- [ ] **Step 7: Commit**

```bash
cd /Users/pj/Documents/hrms
git add apps/admin/ pnpm-lock.yaml
git commit -m "$(cat <<'EOF'
feat(admin): Next.js 15 internal-platform admin shell

Mirror of @hrms/web on port 3002. Robots noindex/nofollow set.
Auth wiring deferred to Phase 1.2.EOF
)"
```

---

## Task 10 — Mobile placeholder + 9 package skeletons

**Files:**

- Create: `apps/mobile/README.md`
- Create: `packages/{domain,contracts,rules-engine,crypto,db,ui,api-client-ts,i18n}/{src/index.ts, package.json, tsconfig.json}`
- Create: `packages/api-client-dart/{README.md, pubspec.yaml.placeholder}`
- Create: `packages/i18n/locales/en.json`

- [ ] **Step 1: Mobile placeholder**

Create `/Users/pj/Documents/hrms/apps/mobile/README.md`:

```markdown
# @hrms/mobile

Flutter mobile app, scaffolded in **Phase 8**.

Folder reserved to keep monorepo structure consistent. Do not place
TypeScript code here — Flutter project with Dart sources will live here.

See `docs/superpowers/specs/2026-05-01-hrms-architecture-design.md` §9.18
for Phase 8 cherries.
```

- [ ] **Step 2: TypeScript-package template (apply to 8 packages)**

For each of these 8 packages:

- `domain`
- `contracts`
- `rules-engine`
- `crypto`
- `db`
- `ui`
- `api-client-ts`
- `i18n`

Create `packages/<name>/package.json`:

```json
{
  "name": "@hrms/<name>",
  "version": "0.0.0",
  "private": true,
  "description": "HRMS <name> package — skeleton, fleshed out in later phases",
  "license": "UNLICENSED",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --max-warnings 0",
    "type": "tsc --noEmit",
    "test": "echo 'tests added in dedicated phase' && exit 0",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@hrms/config": "workspace:*",
    "typescript": "5.6.2"
  }
}
```

(Replace `<name>` with the actual package name in each file.)

Create `packages/<name>/tsconfig.json`:

```json
{
  "extends": "@hrms/config/typescript/base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"]
}
```

Create `packages/<name>/src/index.ts`:

```typescript
// @hrms/<name> — populated in later phases.
// This file exists so Turborepo can resolve the workspace package.
export {};
```

(Each of the 8 needs all three files.)

- [ ] **Step 3: i18n locale skeleton**

Create `/Users/pj/Documents/hrms/packages/i18n/locales/en.json`:

```json
{
  "_meta": {
    "locale": "en",
    "name": "English"
  },
  "common": {
    "loading": "Loading…",
    "save": "Save",
    "cancel": "Cancel"
  }
}
```

- [ ] **Step 4: Dart placeholder**

Create `/Users/pj/Documents/hrms/packages/api-client-dart/README.md`:

```markdown
# @hrms/api-client-dart

Dart client, generated from the OpenAPI spec via `openapi-generator-cli`
in **Phase 8** when the Flutter app comes online.

This folder is reserved.
```

Create `/Users/pj/Documents/hrms/packages/api-client-dart/pubspec.yaml.placeholder`:

```yaml
# Placeholder. Will be replaced with a generated pubspec.yaml in Phase 8.
name: hrms_api_client
description: HRMS Dart client (auto-generated from OpenAPI)
version: 0.0.0
```

- [ ] **Step 5: Install + verify all packages resolve**

```bash
cd /Users/pj/Documents/hrms
pnpm install
pnpm -r exec node -e "console.log(require('./package.json').name)"
```

Expected: every workspace package's name printed.

- [ ] **Step 6: Verify Turborepo recognises the graph**

```bash
pnpm turbo run build --dry=json | head -40
```

Expected: JSON listing tasks for each package — no errors.

- [ ] **Step 7: Commit**

```bash
cd /Users/pj/Documents/hrms
git add apps/mobile/ packages/ pnpm-lock.yaml
git commit -m "$(cat <<'EOF'
chore: scaffold 8 TS package skeletons + 1 Dart placeholder

domain, contracts, rules-engine, crypto, db, ui, api-client-ts, i18n —
each has package.json + tsconfig.json + src/index.ts placeholder.
api-client-dart and apps/mobile are placeholder folders for Phase 8.EOF
)"
```

---

## Task 11 — Docker compose for local dev

**Files:**

- Create: `infra/docker/docker-compose.yml`
- Create: `infra/docker/otel-collector.yaml`

- [ ] **Step 1: docker-compose.yml**

Create `/Users/pj/Documents/hrms/infra/docker/docker-compose.yml`:

```yaml
name: hrms-dev

services:
  postgres:
    image: postgres:16-alpine
    container_name: hrms-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: hrms
      POSTGRES_PASSWORD: hrms_dev_password
      POSTGRES_DB: hrms
    ports:
      - '5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U hrms -d hrms']
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: hrms-redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis-data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 3s
      retries: 5

  meilisearch:
    image: getmeili/meilisearch:v1.10
    container_name: hrms-meilisearch
    restart: unless-stopped
    environment:
      MEILI_MASTER_KEY: hrms_dev_meili_master_key
      MEILI_NO_ANALYTICS: 'true'
      MEILI_ENV: development
    ports:
      - '7700:7700'
    volumes:
      - meilisearch-data:/meili_data
    healthcheck:
      test: ['CMD', 'wget', '-qO-', 'http://localhost:7700/health']
      interval: 10s
      timeout: 5s
      retries: 5

  mailpit:
    image: axllent/mailpit:latest
    container_name: hrms-mailpit
    restart: unless-stopped
    ports:
      - '1025:1025' # SMTP
      - '8025:8025' # Web UI
    environment:
      MP_MAX_MESSAGES: '5000'
      MP_DATABASE: /data/mailpit.db
    volumes:
      - mailpit-data:/data

  minio:
    image: minio/minio:latest
    container_name: hrms-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: hrms_dev
      MINIO_ROOT_PASSWORD: hrms_dev_minio_password
    ports:
      - '9000:9000'
      - '9001:9001'
    volumes:
      - minio-data:/data
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:9000/minio/health/live']
      interval: 10s
      timeout: 5s
      retries: 5

  clamav:
    image: clamav/clamav:1.4
    container_name: hrms-clamav
    restart: unless-stopped
    ports:
      - '3310:3310'
    volumes:
      - clamav-data:/var/lib/clamav
    healthcheck:
      test: ['CMD', 'clamdcheck.sh']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 90s

  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.110.0
    container_name: hrms-otel-collector
    restart: unless-stopped
    command: ['--config=/etc/otel-collector.yaml']
    volumes:
      - ./otel-collector.yaml:/etc/otel-collector.yaml:ro
    ports:
      - '4317:4317' # gRPC
      - '4318:4318' # HTTP
      - '13133:13133' # health check
    depends_on:
      - jaeger

  jaeger:
    image: jaegertracing/all-in-one:1.62
    container_name: hrms-jaeger
    restart: unless-stopped
    environment:
      COLLECTOR_OTLP_ENABLED: 'true'
    ports:
      - '16686:16686' # UI
      - '14250:14250' # Collector

volumes:
  postgres-data:
  redis-data:
  meilisearch-data:
  mailpit-data:
  minio-data:
  clamav-data:
```

- [ ] **Step 2: OTel collector config**

Create `/Users/pj/Documents/hrms/infra/docker/otel-collector.yaml`:

```yaml
receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

processors:
  batch:
    timeout: 1s
    send_batch_size: 1024

  attributes/redact-pii:
    actions:
      - key: pan
        action: delete
      - key: aadhaar
        action: delete
      - key: bank_account
        action: delete
      - key: salary
        action: delete
      - key: dob
        action: delete

extensions:
  health_check:
    endpoint: 0.0.0.0:13133

exporters:
  otlp/jaeger:
    endpoint: jaeger:14250
    tls:
      insecure: true
  debug:
    verbosity: basic

service:
  extensions: [health_check]
  pipelines:
    traces:
      receivers: [otlp]
      processors: [attributes/redact-pii, batch]
      exporters: [otlp/jaeger, debug]
    metrics:
      receivers: [otlp]
      processors: [batch]
      exporters: [debug]
    logs:
      receivers: [otlp]
      processors: [attributes/redact-pii, batch]
      exporters: [debug]
```

- [ ] **Step 3: Start the stack**

```bash
cd /Users/pj/Documents/hrms
pnpm docker:up
```

Expected: 8 containers start (postgres, redis, meilisearch, mailpit, minio, clamav, otel-collector, jaeger). ClamAV takes ~60-90s on first run to download virus DB.

- [ ] **Step 4: Verify each service is healthy**

```bash
docker compose -f infra/docker/docker-compose.yml ps
```

Expected: all containers `running`. ClamAV may show `health: starting` for the first 90s.

```bash
# Postgres
docker exec hrms-postgres pg_isready -U hrms

# Redis
docker exec hrms-redis redis-cli ping

# Meilisearch
curl -s http://localhost:7700/health

# Mailpit
curl -s http://localhost:8025/api/v1/info | head -c 100

# MinIO
curl -s http://localhost:9000/minio/health/live -o /dev/null -w "%{http_code}\n"

# Jaeger UI
curl -s http://localhost:16686/ -o /dev/null -w "%{http_code}\n"

# OTel collector health
curl -s http://localhost:13133/ -o /dev/null -w "%{http_code}\n"
```

Expected each: 200 / `accepting connections` / `PONG` / `{"status":"available"}`.

- [ ] **Step 5: Stop the stack (we'll start it via setup script next)**

```bash
pnpm docker:down
```

- [ ] **Step 6: Commit**

```bash
git add infra/docker/
git commit -m "$(cat <<'EOF'
chore: add docker-compose for local dev (Postgres, Redis, Meilisearch, Mailpit, MinIO, ClamAV, OTel, Jaeger)

OTel collector configured with PII redaction processor (per-spec §6.14).
Volumes persisted; healthchecks on every service. Mailpit replaces real
email; MinIO replaces real R2.EOF
)"
```

---

## Task 12 — Setup, reset, and verify scripts

**Files:**

- Create: `scripts/setup.sh`
- Create: `scripts/dev-reset.sh`
- Create: `scripts/verify-setup.sh`

- [ ] **Step 1: scripts/setup.sh**

Create `/Users/pj/Documents/hrms/scripts/setup.sh`:

```bash
#!/usr/bin/env bash
set -euo pipefail

readonly REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$REPO_ROOT"

step() { printf '\n\033[1;36m▶ %s\033[0m\n' "$*"; }
ok()   { printf '  \033[1;32m✓ %s\033[0m\n' "$*"; }
fail() { printf '  \033[1;31m✗ %s\033[0m\n' "$*"; exit 1; }

step "Verifying tool versions"
node -e "const v = process.versions.node.split('.').map(Number); if (v[0] < 20) { console.error('Node 20+ required, got ' + process.version); process.exit(1); }"
ok "Node $(node -v)"

command -v pnpm >/dev/null 2>&1 || fail "pnpm not installed (npm i -g pnpm@9)"
ok "pnpm $(pnpm -v)"

command -v docker >/dev/null 2>&1 || fail "docker not installed"
docker info >/dev/null 2>&1 || fail "docker daemon not running"
ok "Docker $(docker --version | awk '{print $3}' | tr -d ',')"

step "Installing dependencies"
pnpm install
ok "deps installed"

step "Starting Docker services"
docker compose -f infra/docker/docker-compose.yml up -d
ok "containers up"

step "Waiting for services to be healthy"
WAIT_SECONDS=120
START=$(date +%s)
while true; do
  ALL_HEALTHY=true
  for svc in postgres redis meilisearch minio; do
    STATUS=$(docker inspect -f '{{.State.Health.Status}}' "hrms-$svc" 2>/dev/null || echo "missing")
    if [ "$STATUS" != "healthy" ]; then
      ALL_HEALTHY=false
    fi
  done
  if [ "$ALL_HEALTHY" = "true" ]; then
    ok "all core services healthy"
    break
  fi
  ELAPSED=$(($(date +%s) - START))
  if [ "$ELAPSED" -gt "$WAIT_SECONDS" ]; then
    fail "services not healthy after ${WAIT_SECONDS}s (clamav can be slow on first run; ignore if only it is starting)"
  fi
  sleep 2
done

step "Building all packages"
pnpm build
ok "build green"

step "Setup complete"
cat <<'EOF'

Ready:
  Web:        http://localhost:3000   (pnpm --filter @hrms/web dev)
  Admin:      http://localhost:3002   (pnpm --filter @hrms/admin dev)
  API:        http://localhost:3001   (pnpm --filter @hrms/api dev)
  Mailpit:    http://localhost:8025
  Meilisearch:http://localhost:7700
  MinIO:      http://localhost:9001   (root: hrms_dev / hrms_dev_minio_password)
  Jaeger:     http://localhost:16686
  Postgres:   postgres://hrms:hrms_dev_password@localhost:5432/hrms
  Redis:      redis://localhost:6379

Run `pnpm dev` to start everything in parallel.

EOF
```

```bash
chmod +x scripts/setup.sh
```

- [ ] **Step 2: scripts/dev-reset.sh**

Create `/Users/pj/Documents/hrms/scripts/dev-reset.sh`:

```bash
#!/usr/bin/env bash
set -euo pipefail

readonly REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$REPO_ROOT"

echo "▶ Stopping containers and deleting volumes (data will be lost)"
docker compose -f infra/docker/docker-compose.yml down -v

echo "▶ Removing build artifacts"
pnpm clean

echo "▶ Reinstalling dependencies"
pnpm install

echo "▶ Restarting via setup.sh"
exec bash scripts/setup.sh
```

```bash
chmod +x scripts/dev-reset.sh
```

- [ ] **Step 3: scripts/verify-setup.sh (smoke test)**

Create `/Users/pj/Documents/hrms/scripts/verify-setup.sh`:

```bash
#!/usr/bin/env bash
set -euo pipefail

readonly REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$REPO_ROOT"

PASS=0
FAIL=0

check() {
  if eval "$2" >/dev/null 2>&1; then
    printf '  \033[1;32m✓\033[0m %s\n' "$1"
    PASS=$((PASS+1))
  else
    printf '  \033[1;31m✗\033[0m %s\n' "$1"
    FAIL=$((FAIL+1))
  fi
}

echo "▶ Verifying setup"
check "Node 20+"                        "node -e 'process.exit(process.versions.node.split(\".\").map(Number)[0] >= 20 ? 0 : 1)'"
check "pnpm 9+"                         "[ \"$(pnpm -v | cut -d. -f1)\" -ge 9 ]"
check "Docker daemon up"                "docker info"
check "Postgres responding"             "docker exec hrms-postgres pg_isready -U hrms"
check "Redis responding"                "docker exec hrms-redis redis-cli ping | grep -q PONG"
check "Meilisearch healthy"             "curl -fsS http://localhost:7700/health"
check "MinIO healthy"                   "curl -fsS http://localhost:9000/minio/health/live"
check "Jaeger UI reachable"             "curl -fsS http://localhost:16686/"
check "OTel collector healthy"          "curl -fsS http://localhost:13133/"
check "Mailpit reachable"               "curl -fsS http://localhost:8025/api/v1/info"
check "API package builds"              "pnpm --filter @hrms/api build"
check "Workers package builds"          "pnpm --filter @hrms/workers build"
check "Web package builds"              "pnpm --filter @hrms/web build"
check "Admin package builds"            "pnpm --filter @hrms/admin build"

echo ""
echo "Passed: $PASS  Failed: $FAIL"
[ "$FAIL" -eq 0 ] || exit 1
```

```bash
chmod +x scripts/verify-setup.sh
```

- [ ] **Step 4: Run setup end-to-end from clean state**

```bash
cd /Users/pj/Documents/hrms
pnpm docker:down
pnpm setup
```

Expected: setup.sh runs through all steps without error and prints the "Ready" panel.

- [ ] **Step 5: Run verify-setup**

```bash
pnpm verify
```

Expected: all checks `✓`. ClamAV check is intentionally absent because of slow first-run behavior; the docker:down/up cycle in setup re-uses the cached DB so subsequent runs are instant.

- [ ] **Step 6: Commit**

```bash
git add scripts/
git commit -m "$(cat <<'EOF'
chore: add setup, dev-reset, verify-setup scripts

scripts/setup.sh — verify tools, install, start docker, wait for health, build
scripts/dev-reset.sh — destructive reset of containers + volumes + node_modules
scripts/verify-setup.sh — read-only smoke test of every layerEOF
)"
```

---

## Task 13 — VS Code workspace

**Files:**

- Create: `.vscode/settings.json`
- Create: `.vscode/extensions.json`

- [ ] **Step 1: Settings**

Create `/Users/pj/Documents/hrms/.vscode/settings.json`:

```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": "explicit",
    "source.organizeImports": "explicit"
  },
  "typescript.tsdk": "node_modules/typescript/lib",
  "typescript.preferences.preferTypeOnlyAutoImports": true,
  "typescript.suggest.autoImports": true,
  "typescript.updateImportsOnFileMove.enabled": "always",
  "typescript.format.enable": false,
  "javascript.format.enable": false,
  "eslint.workingDirectories": [{ "pattern": "apps/*" }, { "pattern": "packages/*" }],
  "eslint.useFlatConfig": true,
  "files.exclude": {
    "**/node_modules": true,
    "**/.turbo": true,
    "**/.next": true,
    "**/dist": true
  },
  "search.exclude": {
    "**/node_modules": true,
    "**/.turbo": true,
    "**/.next": true,
    "**/dist": true,
    "**/pnpm-lock.yaml": true
  },
  "files.associations": {
    "*.css": "css"
  },
  "[markdown]": {
    "editor.formatOnSave": false
  }
}
```

- [ ] **Step 2: Recommended extensions**

Create `/Users/pj/Documents/hrms/.vscode/extensions.json`:

```json
{
  "recommendations": [
    "esbenp.prettier-vscode",
    "dbaeumer.vscode-eslint",
    "bradlc.vscode-tailwindcss",
    "yoavbls.pretty-ts-errors",
    "usernamehw.errorlens",
    "vitest.explorer",
    "christian-kohler.path-intellisense",
    "redhat.vscode-yaml",
    "tamasfe.even-better-toml",
    "ms-azuretools.vscode-docker",
    "github.vscode-github-actions",
    "eamodio.gitlens",
    "gruntfuggly.todo-tree",
    "streetsidesoftware.code-spell-checker"
  ]
}
```

- [ ] **Step 3: Commit**

```bash
git add .vscode/
git commit -m "$(cat <<'EOF'
chore: add VS Code workspace settings + recommended extensions

Format-on-save with Prettier, ESLint flat-config, type-only auto-imports,
search excludes for node_modules/.turbo/dist.EOF
)"
```

---

## Task 14 — GitHub Actions CI workflow

**Files:**

- Create: `.github/workflows/ci.yml`

- [ ] **Step 1: Write the CI workflow**

Create `/Users/pj/Documents/hrms/.github/workflows/ci.yml`:

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: 20.18.0
  PNPM_VERSION: 9.12.0

jobs:
  install:
    name: Install + cache
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

  lint:
    name: Lint + format
    needs: install
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm format:check
      - run: pnpm lint

  type:
    name: Typecheck
    needs: install
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm type

  test:
    name: Test
    needs: install
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm test

  build:
    name: Build
    needs: install
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm build

  secrets-scan:
    name: Secrets scan (gitleaks)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v2
        env:
          GITLEAKS_CONFIG: .gitleaks.toml

  security-deps:
    name: Dependency vulnerabilities
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: pnpm audit (high+)
        run: pnpm audit --audit-level=high || true
      - name: osv-scanner
        uses: google/osv-scanner-action/osv-scanner-action@v1.9.1
        with:
          scan-args: |-
            --recursive
            --skip-git
            ./
```

- [ ] **Step 2: Verify the workflow file is valid yaml**

```bash
cd /Users/pj/Documents/hrms
docker run --rm -v "$PWD:/work" -w /work cytopia/yamllint:latest .github/workflows/ci.yml || \
  echo "(yamllint failed — install via brew if you want stricter checks; this is non-blocking for plan execution)"
```

(Non-blocking — visual inspection of the YAML in editor is sufficient if yamllint is unavailable.)

- [ ] **Step 3: Commit**

```bash
git add .github/
git commit -m "$(cat <<'EOF'
ci: add GitHub Actions baseline pipeline

Jobs: install (cache key), lint+format, typecheck, test, build,
gitleaks secrets scan, dependency vulnerability scan (pnpm audit + osv-scanner).
Concurrency cancels superseded PR runs.EOF
)"
```

(Note: this workflow only runs once you push to a GitHub remote. For Phase 1.0 local validation, the next step verifies all gates locally.)

---

## Task 15 — ADR template + first 2 ADRs

**Files:**

- Create: `docs/adr/README.md`
- Create: `docs/adr/_template.md`
- Create: `docs/adr/0001-modular-monolith.md`
- Create: `docs/adr/0002-postgres-with-rls.md`

- [ ] **Step 1: ADR README**

Create `/Users/pj/Documents/hrms/docs/adr/README.md`:

```markdown
# Architecture Decision Records

ADRs document **why** a non-trivial decision was made — not what the
code does (the code does that). The audience is future-you, future
collaborators, and auditors.

## Conventions

- One file per decision, numbered: `NNNN-short-slug.md`
- Status: `Proposed` → `Accepted` → `Superseded by NNNN`
- Never delete; supersede.
- Link from PR descriptions and code comments where the decision matters.

## Index

- [0001 — Modular monolith over microservices](./0001-modular-monolith.md)
- [0002 — Postgres with RLS over Mongo](./0002-postgres-with-rls.md)

Add new ADRs by copying [`_template.md`](./_template.md).
```

- [ ] **Step 2: Template**

Create `/Users/pj/Documents/hrms/docs/adr/_template.md`:

```markdown
# NNNN — Short title

|         |                                          |
| ------- | ---------------------------------------- |
| Status  | Proposed / Accepted / Superseded by NNNN |
| Date    | YYYY-MM-DD                               |
| Decider | Founder                                  |
| Related | links to PRs, ADRs, spec sections        |

## Context

What is the problem? What constraints apply? What did we already know?

## Decision

What did we decide?

## Consequences

What are the effects of this decision — good, bad, neutral?

## Alternatives considered

What did we _not_ choose? Why?
```

- [ ] **Step 3: ADR 0001**

Create `/Users/pj/Documents/hrms/docs/adr/0001-modular-monolith.md`:

```markdown
# 0001 — Modular monolith over microservices

|         |                                                                                                                                                                                                                                                 |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Status  | Accepted                                                                                                                                                                                                                                        |
| Date    | 2026-05-01                                                                                                                                                                                                                                      |
| Decider | Founder                                                                                                                                                                                                                                         |
| Related | [Spec §3 Bounded contexts](../superpowers/specs/2026-05-01-hrms-architecture-design.md#3--bounded-contexts-modules), [Spec §4 Module pattern](../superpowers/specs/2026-05-01-hrms-architecture-design.md#5--module-internal-pattern-hexagonal) |

## Context

8-phase HRMS over 18-24 months. Solo founder + AI assistance. Need to:

- Hold shape across long delivery
- Keep modules independently testable
- Make future Go-extraction of hot paths (payroll, rules engine) mechanical

Three options weighed: boring monolith, modular monolith, microservices.

## Decision

Modular monolith. Each bounded context lives in `apps/api/src/modules/<name>/`,
owns its own Postgres schema, exposes its public surface via
`packages/contracts/<name>/`, and talks to other modules only through those
contracts or via the transactional outbox.

Hard import rules enforced by `dependency-cruiser` and architecture fitness
function tests in CI.

## Consequences

**Good**

- One deploy, one log stream, one DB transaction across modules where needed.
- Modules independently testable (hexagonal pattern in §5 of spec).
- Solo dev never spends time on inter-service plumbing.
- Future Go extraction is a port-swap, not a rewrite.

**Bad**

- Requires upfront discipline (~15-20% extra setup time in Phase 1).
- Misses some scale ceiling vs microservices — irrelevant at 1M employees.

## Alternatives considered

- **Boring monolith** — by Phase 5 cross-module imports would be untangled
  manually; explicitly the "garbage code" outcome the founder rejected.
- **Microservices from day one** — solo dev + 8 phases + microservices = doom.
  Distributed transactions on payroll alone would exceed the entire schedule.
```

- [ ] **Step 4: ADR 0002**

Create `/Users/pj/Documents/hrms/docs/adr/0002-postgres-with-rls.md`:

```markdown
# 0002 — Postgres with Row-Level Security over MongoDB

|         |                                                                                                                                                                                                                        |
| ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Status  | Accepted                                                                                                                                                                                                               |
| Date    | 2026-05-01                                                                                                                                                                                                             |
| Decider | Founder                                                                                                                                                                                                                |
| Related | [Spec §1 Decisions](../superpowers/specs/2026-05-01-hrms-architecture-design.md#1--decisions-locked), [Spec §6 Database](../superpowers/specs/2026-05-01-hrms-architecture-design.md#6--database-design-postgresql-16) |

## Context

The original spec (`spec/00-foundations/01-multi-tenancy.md`) chose MongoDB
because of Node/Mongoose ergonomics. Switching to NestJS + TypeScript-first
stack gave us the option to reconsider.

Multi-tenant compliance-heavy product needs:

- Strongest possible tenant isolation
- Time-versioning for statutory data (retros, audits)
- Append-only audit log
- Mature managed offerings

## Decision

PostgreSQL 16+ with **Row-Level Security (RLS) FORCED** as the primary
multi-tenancy mechanism. One schema per bounded context. Per-schema Postgres
roles for least-privilege defense in depth. Bitemporal modeling via range
types + exclusion constraints.

## Consequences

**Good**

- RLS at the DB layer means even a SQL-injected query cannot cross tenants.
- Bitemporal exclusion constraints make overlapping records mathematically
  impossible.
- Strict schema = compliance audit answer is "look at migration history."
- pg_partman, BRIN, logical replication scale fine to ceiling (~1M employees).

**Bad**

- Migration discipline higher than Mongo (offset by expand-contract pattern).
- Connection pooling matters earlier (PgBouncer in transaction mode in P4+).

## Alternatives considered

- **MongoDB** — original spec choice; weaker isolation primitives in Mongo;
  bitemporal modeling is hand-rolled rather than DB-enforced.
- **MariaDB / MySQL** — RLS support weaker; bitemporal modeling weaker.
- **CockroachDB** — overkill at our scale; ops complexity not warranted.
```

- [ ] **Step 5: Commit**

```bash
git add docs/adr/
git commit -m "$(cat <<'EOF'
docs(adr): add ADR template + first 2 ADRs (modular monolith, Postgres RLS)

ADRs document WHY non-trivial decisions were made. Numbered files,
never deleted, only superseded.EOF
)"
```

---

## Task 16 — Onboarding documentation

**Files:**

- Create: `README.md`
- Create: `docs/dev/onboarding.md`
- Create: `docs/dev/troubleshooting.md`

- [ ] **Step 1: Repo README**

Create `/Users/pj/Documents/hrms/README.md`:

````markdown
# HRMS

Indian compliance-first multi-tenant HR platform — modular monolith on
NestJS + Next.js + Flutter, backed by PostgreSQL with Row-Level Security.

> Phase 1.0 (monorepo bootstrap) is **in progress**. See
> [`docs/superpowers/plans/2026-05-01-p1.0-monorepo-bootstrap.md`](./docs/superpowers/plans/2026-05-01-p1.0-monorepo-bootstrap.md).

## Get started in 5 minutes

```bash
git clone <repo-url> hrms
cd hrms

# Prereqs (one-time): Node 20+, pnpm 9+, Docker, gitleaks (optional)
node -v && pnpm -v && docker info

# Bootstrap everything
pnpm setup

# Verify
pnpm verify

# Start everything in parallel
pnpm dev
```
````

URLs after `pnpm dev`:

| Service                       | URL                    |
| ----------------------------- | ---------------------- |
| Web (customer + tenant-admin) | http://localhost:3000  |
| API                           | http://localhost:3001  |
| Admin (internal)              | http://localhost:3002  |
| Mailpit                       | http://localhost:8025  |
| Meilisearch                   | http://localhost:7700  |
| MinIO console                 | http://localhost:9001  |
| Jaeger UI                     | http://localhost:16686 |

## Documentation

- **[Architecture design](./docs/superpowers/specs/2026-05-01-hrms-architecture-design.md)** — the full system design (~17k words; sections 1-13 + appendices A-H)
- **[ADRs](./docs/adr/)** — every "why" lives here
- **[Onboarding](./docs/dev/onboarding.md)** — first-day checklist
- **[Troubleshooting](./docs/dev/troubleshooting.md)** — common issues + fixes
- **[Specs](./spec/)** — original 10-module functional spec
- **[Plans](./docs/superpowers/plans/)** — implementation plans, one per sub-phase

## Repository structure

```
apps/        # api (NestJS) · workers · web (Next.js) · admin · mobile (Phase 8)
packages/    # domain · contracts · rules-engine · crypto · db · ui · api-client-{ts,dart} · i18n · config
infra/       # docker · coolify
scripts/     # setup · dev-reset · verify-setup
docs/        # adr · dev · superpowers/specs · superpowers/plans
spec/        # original 10-module functional spec
```

## License

UNLICENSED. Proprietary. All rights reserved.

````

- [ ] **Step 2: Onboarding doc**

Create `/Users/pj/Documents/hrms/docs/dev/onboarding.md`:

```markdown
# Developer onboarding

> Goal: be productive in 30 minutes.

## 1. Tools (one-time)

| Tool | Version | Install |
|---|---|---|
| Node | 20.18.0 | `mise install`, or `nvm install`, or `fnm install` (uses `.nvmrc` / `.node-version`) |
| pnpm | 9+ | `npm install -g pnpm@9` |
| Docker | latest | Docker Desktop or Colima (`brew install colima docker`) |
| gitleaks | latest | `brew install gitleaks` (macOS) — recommended but not required |

Set git config (one-time):

```bash
git config --global user.name  "Your Name"
git config --global user.email "you@example.com"
````

## 2. Bootstrap

```bash
git clone <repo-url> hrms
cd hrms
pnpm setup        # installs deps, starts docker, builds
pnpm verify       # smoke test
```

If `pnpm setup` fails, see [troubleshooting](./troubleshooting.md).

## 3. Daily workflow

```bash
pnpm dev          # start everything in parallel (Turborepo TUI)
pnpm test         # run tests
pnpm lint         # lint + format check
pnpm type         # typecheck
pnpm build        # full production build
```

Working on one app:

```bash
pnpm --filter @hrms/api dev
pnpm --filter @hrms/web dev
```

Reset state when something is wedged:

```bash
pnpm dev:reset    # destructive: nukes containers + node_modules
```

## 4. Commit conventions

Conventional commits enforced by commitlint:

```
feat(scope): short summary in lowercase
fix(scope): another summary
chore: maintenance work
docs: documentation only
refactor: code change without behavior change
test: adding/fixing tests
ci: CI changes
build: build system changes
perf: performance improvement
security: security fix or hardening
```

Pre-commit hooks auto-format changed files via Prettier.
gitleaks scans staged changes for secrets.

## 5. Where to find things

| Question                          | Answer                                                          |
| --------------------------------- | --------------------------------------------------------------- |
| Architecture overview             | `docs/superpowers/specs/2026-05-01-hrms-architecture-design.md` |
| Why X was decided                 | `docs/adr/`                                                     |
| Bounded context map               | Spec §3                                                         |
| Database conventions              | Spec §6                                                         |
| Cross-cutting (auth, audit, etc.) | Spec §7                                                         |
| Testing strategy                  | Spec §8                                                         |
| API design conventions            | Spec Appendix E                                                 |
| Local dev commands                | Spec Appendix H + this doc                                      |

## 6. First contribution

1. Pick a checkbox from `docs/superpowers/plans/2026-05-01-p1.0-monorepo-bootstrap.md`
2. Create a branch: `git checkout -b feat/<short-slug>`
3. Make the change with tests
4. Run `pnpm test && pnpm lint && pnpm type && pnpm build`
5. Commit (conventional message); push; open PR

````

- [ ] **Step 3: Troubleshooting doc**

Create `/Users/pj/Documents/hrms/docs/dev/troubleshooting.md`:

```markdown
# Troubleshooting

## `pnpm setup` fails

### Error: `docker info` exits non-zero
Docker daemon is not running. Start Docker Desktop or `colima start`.

### Error: `services not healthy after 120s`
ClamAV is slow on first run (downloads virus DB ~250MB). It is excluded from
the wait loop in `scripts/setup.sh`, but if other services are slow:

```bash
docker compose -f infra/docker/docker-compose.yml ps
docker compose -f infra/docker/docker-compose.yml logs <service>
````

Common cause: port already in use. Stop the conflicting process or change
ports in `infra/docker/docker-compose.yml`.

### Error: `Node 20+ required`

Use a Node version manager. With `mise` (recommended): `mise install`.
With `nvm`: `nvm install && nvm use`.

## `pnpm install` fails with peer dependency warnings

Set `auto-install-peers=true` is already in `.npmrc`. If a specific package
warns: search for it on https://github.com/pnpm/pnpm/issues — usually a known
transient issue.

## Pre-commit hook fails

### `gitleaks` not found

Install: `brew install gitleaks` (macOS) or download from
https://github.com/gitleaks/gitleaks/releases.

The hook gracefully skips if gitleaks isn't installed, so this is a _warning_
not a failure. CI runs gitleaks regardless.

### `commitlint` rejects message

Use a conventional commit message: `feat(api): your message`. See
[onboarding](./onboarding.md#4-commit-conventions).

## `pnpm dev` complains about port already in use

```bash
# Find what's holding the port:
lsof -i :3000
# Kill it:
kill <pid>
```

## API health endpoint returns 502 / connection refused

The API didn't start. Check logs:

```bash
pnpm --filter @hrms/api dev
```

Look for TS compilation errors or missing env vars.

## Postgres won't start

Existing volume corrupted. Reset:

```bash
pnpm dev:reset
```

(Destructive — wipes all local DB data.)

## VS Code: "Cannot find module '@hrms/config/...' "

Run `pnpm install` once more. Then in VS Code: Cmd+Shift+P → "TypeScript: Restart TS server".

## CI passes locally but fails on GitHub

Most common: lockfile drift. Run `pnpm install` and commit `pnpm-lock.yaml`.

## Tests pass locally but fail in CI

Most common: timezone mismatch. Either the test depends on local timezone
(fix the test to use UTC) or the CI environment has stricter type checking
(`pnpm type` should catch this locally).

````

- [ ] **Step 4: Verify links**

Manually open the README in VS Code preview; click each link to confirm it points to a real file.

- [ ] **Step 5: Commit**

```bash
git add README.md docs/dev/
git commit -m "$(cat <<'EOF'
docs: add README, onboarding, troubleshooting

README — 5-min get-started + URL table + repo structure
onboarding.md — first-day checklist, daily workflow, commit conventions
troubleshooting.md — every "fail in setup" path with fixEOF
)"
````

---

## Task 17 — End-to-end verification + final tag

- [ ] **Step 1: Clean state and re-bootstrap**

```bash
cd /Users/pj/Documents/hrms
pnpm docker:down -v
rm -rf node_modules apps/*/node_modules apps/*/dist apps/*/.next packages/*/node_modules packages/*/dist
pnpm setup
```

Expected: setup script completes, prints the "Ready" panel.

- [ ] **Step 2: Run the verify script**

```bash
pnpm verify
```

Expected: every check `✓`, `Failed: 0`.

- [ ] **Step 3: Run all CI gates locally**

```bash
pnpm format:check
pnpm lint
pnpm type
pnpm test
pnpm build
```

Expected: all green.

- [ ] **Step 4: Manual smoke of each app**

In separate terminals:

```bash
pnpm --filter @hrms/api dev
# wait for "listening on http://localhost:3001"
```

```bash
pnpm --filter @hrms/web dev
# wait for "Local: http://localhost:3000"
```

```bash
pnpm --filter @hrms/admin dev
# wait for "Local: http://localhost:3002"
```

```bash
pnpm --filter @hrms/workers dev
# wait for "[workers] HRMS workers process started"
```

In a fifth terminal:

```bash
curl -s http://localhost:3001/health         | head -c 200; echo
curl -s http://localhost:3001/health/liveness| head -c 200; echo
curl -s http://localhost:3000               -o /dev/null -w "web: HTTP %{http_code}\n"
curl -s http://localhost:3002               -o /dev/null -w "admin: HTTP %{http_code}\n"
```

Expected output:

```
{"status":"ok","uptime":12.34,"version":"0.0.0-dev","timestamp":"2026-..."}
{"status":"ok"}
web: HTTP 200
admin: HTTP 200
```

Stop all dev servers (Ctrl+C in each terminal).

- [ ] **Step 5: Sanity-check git history**

```bash
git log --oneline | head -20
```

Expected: 14-15 commits, each conventional-format.

- [ ] **Step 6: Confirm git status is clean**

```bash
git status
```

Expected: `nothing to commit, working tree clean`.

- [ ] **Step 7: Tag the completion of Phase 1.0**

```bash
git tag -a p1.0-complete -m "Phase 1.0 monorepo bootstrap — complete"
git tag --list
```

Expected: `p1.0-complete` and `p1.0-start` both listed.

- [ ] **Step 8: Push everything to GitHub**

```bash
cd /Users/pj/Documents/hrms
git push -u origin main
git push origin --tags
```

Expected output:

- Branch `main` set up to track `origin/main`
- Tags `p1.0-start` and `p1.0-complete` pushed
- GitHub Actions CI workflow triggers automatically (visible at https://github.com/pankaj1989/HRMS/actions)

If push fails with permission denied (publickey):

```bash
ssh -T git@github.com
```

Should print "Hi pankaj1989! You've successfully authenticated...". If not, set up an SSH key and add it to GitHub before retrying.

- [ ] **Step 9: Watch the first CI run**

Open https://github.com/pankaj1989/HRMS/actions in a browser. Confirm all jobs complete green:

- install
- lint
- type
- test
- build
- secrets-scan
- security-deps

If any job fails, the failure is the actual P1.0 incomplete state — fix locally and force-push or open a fix commit. Do not move on to Phase 1.1 with a red CI.

- [ ] **Step 10: Done — write a "what shipped" note**

Append to `/Users/pj/Documents/hrms/docs/superpowers/plans/2026-05-01-p1.0-monorepo-bootstrap.md` (the bottom of this very file):

```markdown
---

## Completion record

**Tag:** `p1.0-complete`
**Date:** YYYY-MM-DD (fill in actual completion date)
**Commits:** ~15

### What shipped

- Monorepo with pnpm workspaces + Turborepo
- 4 apps (api, workers, web, admin) + 8 TS packages + 1 Dart placeholder + mobile placeholder
- Docker compose for Postgres, Redis, Meilisearch, Mailpit, MinIO, ClamAV, OTel collector, Jaeger
- Pre-commit hooks: husky + lint-staged + commitlint + gitleaks
- CI pipeline: install, lint, type, test, build, secrets scan, dependency vuln scan
- ADR template + 2 first ADRs
- Onboarding + troubleshooting docs
- One green API endpoint: `GET /health`

### Verified

- ✓ `pnpm setup` succeeds from clean checkout
- ✓ `pnpm verify` all green
- ✓ `pnpm dev` starts all four apps
- ✓ All CI gates green locally (lint, type, test, build, format)
- ✓ Conventional commit format enforced
- ✓ Pre-commit secret scan fires on test
- ✓ Hooks block malformed commit messages

### Known limitations (intentional, deferred to later phases)

- No DB schema yet (Phase 1.1)
- No auth yet (Phase 1.2)
- No outbox / sagas (Phase 1.3)
- No rules engine (Phase 1.4)
- No documents / notifications wiring (Phase 1.5)
- No production deploy pipeline (Phase 1.6)
- No mobile app (Phase 8)

### Next plan

`docs/superpowers/plans/<date>-p1.1-database-foundations.md`
```

Commit the completion record:

```bash
git add docs/superpowers/plans/2026-05-01-p1.0-monorepo-bootstrap.md
git commit -m "$(cat <<'EOF'
docs(plans): record P1.0 completion in plan file

Verified end-to-end. Ready for Phase 1.1 (database foundations).EOF
)"
git tag -fa p1.0-complete -m "Phase 1.0 monorepo bootstrap — complete (with completion record)"
```

---

## Self-review checklist (run before declaring P1.0 done)

- [ ] Every file in the "File structure" section above exists
- [ ] `pnpm setup` succeeds from a fresh `git clone`
- [ ] `pnpm verify` returns `Failed: 0`
- [ ] `pnpm format:check`, `pnpm lint`, `pnpm type`, `pnpm test`, `pnpm build` all green
- [ ] Pre-commit hook auto-formats a deliberately-mangled file
- [ ] commitlint blocks a non-conventional message
- [ ] gitleaks scan runs (or warns) on every commit
- [ ] CI workflow file is valid YAML (visually verified or yamllint-passed)
- [ ] All 4 apps boot via `pnpm dev` without errors
- [ ] `GET /health` returns the documented JSON shape
- [ ] All 9 TS package skeletons resolve (`pnpm -r exec node -e ...`)
- [ ] ADR 0001 + 0002 are checked in and referenced from README
- [ ] Onboarding + troubleshooting docs link correctly to spec sections
- [ ] `git tag --list` shows `p1.0-start` and `p1.0-complete`
- [ ] `git status` is clean
- [ ] The "Completion record" section above is filled in with the actual date

---

_Plan total: 17 tasks, ~95 individually checkable steps._

---

## Completion record

**Status:** ✅ Complete
**Tag:** `p1.0-complete`
**Date completed:** 2026-05-01
**Total commits since `p1.0-start`:** 21
**Author on every commit:** `pankaj1989 <pankaj.adhikari1989@gmail.com>`
**`Co-Authored-By:` lines in any commit:** 0

### Commit log (since checkpoint)

| Task | Subject                                                                                          | SHA     |
| ---- | ------------------------------------------------------------------------------------------------ | ------- |
| 4    | `chore: add Prettier, ESLint (typescript-eslint strict), commitlint, gitleaks`                   | 75502eb |
| 5    | `chore: add husky + lint-staged + commitlint pre-commit hooks`                                   | c7d632b |
| 6    | `feat(api): NestJS skeleton with health endpoint`                                                | 110dcd4 |
| 7    | `feat(workers): NestJS workers process skeleton`                                                 | e13049f |
| 8    | `feat(web): Next.js 15 customer/tenant-admin shell`                                              | 16fc98a |
| 9    | `feat(admin): Next.js 15 internal-platform admin shell`                                          | c03c0a1 |
| 10   | `chore: scaffold 8 TS package skeletons + 1 Dart placeholder`                                    | d87e120 |
| 11   | `chore: add docker-compose for local dev (postgres, redis, meili, mailpit, minio, clamav, otel)` | a4c2f55 |
| 12   | `chore: add setup, dev-reset, verify-setup scripts`                                              | dbcaf0a |
| 13   | `chore: add VS Code workspace settings + recommended extensions`                                 | 6d709f4 |
| 14   | `ci: add GitHub Actions baseline pipeline`                                                       | dd44528 |
| 15   | `docs(adr): add ADR template + first 2 ADRs (modular monolith, Postgres RLS)`                    | e195083 |
| 16   | `docs: add README, onboarding, troubleshooting`                                                  | d201e1d |
| Fix  | `fix: rename pnpm script "setup" to "bootstrap" — avoid builtin collision`                       | fa10f1d |
| Fix  | `fix(infra): map host ports 5433/6380 for postgres/redis to avoid local conflicts`               | 76eacea |
| Fix  | `fix(infra): meilisearch healthcheck use 127.0.0.1 instead of localhost`                         | 9fb0363 |
| Fix  | `fix(lint): satisfy CI gates across all 12 workspace packages`                                   | d5ff0b9 |

### Final regression results

- ✅ `pnpm verify` — 14/14 checks pass (Node, pnpm, Docker daemon, Postgres, Redis, Meilisearch, MinIO, Jaeger, OTel collector, Mailpit, all 4 app builds)
- ✅ `pnpm format:check` — all files Prettier-formatted
- ✅ `pnpm lint` — 12/12 packages
- ✅ `pnpm type` — 12/12 packages (strict mode + `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes`)
- ✅ `pnpm test` — 12/12 packages (NestJS API e2e: 2/2 green; others: no tests yet)
- ✅ `pnpm build` — 12/12 packages
- ✅ All 8 docker services brought up healthy from clean state
- ✅ pre-commit hooks fire on every commit (lint-staged + gitleaks)
- ✅ commit-msg hook enforces conventional format

### Plan deviations (intentional, fixed during execution)

| Issue surfaced                                                                                                                                  | Fix                                                                                                                      |
| ----------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| Plan said `"prettier": "@hrms/config/prettier"` but Prettier 3 ESM resolver doesn't auto-resolve directory→`index.cjs`                          | Updated to `@hrms/config/prettier/index.cjs`                                                                             |
| Plan called `pnpm setup` — collides with reserved pnpm built-in (configures shell PATH)                                                         | Renamed to `pnpm bootstrap` everywhere                                                                                   |
| Postgres/Redis default ports (5432/6379) collided with other dev services on host                                                               | Mapped to 5433/6380 (host) — internal container port unchanged                                                           |
| Meilisearch healthcheck `wget http://localhost:7700/health` failed: container's `localhost` is IPv6 ::1 but Meilisearch binds IPv4 0.0.0.0 only | Changed healthcheck to `127.0.0.1:7700`                                                                                  |
| `apps/api/tsconfig.json` inherited `rootDir: ${configDir}/src` from base, but `include` covers `test/**/*` (outside src)                        | Override `rootDir: "./"` for type-check; build-only `rootDir: "./src"` moved to `tsconfig.build.json`                    |
| ESLint 9 requires flat config (`eslint.config.{js,cjs,mjs}`); plan didn't provide them                                                          | Added per-package `eslint.config.cjs` re-exporting shared config; Next apps got `.eslintrc.json` (Next 15 supports both) |
| `apps/api/test/health.e2e-spec.ts` had untyped supertest body access flagged by `strictTypeChecked`                                             | Rewrote with explicit type annotations + `typeof` assertions instead of `expect.any()`                                   |
| `apps/workers/src/main.ts` used `console.log` (forbidden by no-console rule allow-list `[warn, error]`)                                         | Switched to `console.warn` (placeholder until Phase 1.3 wires Nest Logger)                                               |

### Verified

- ✓ `pnpm bootstrap` succeeds from clean checkout (deps install + 8 docker services up + builds green)
- ✓ `pnpm verify` all 14 checks pass
- ✓ `pnpm dev` starts all 4 apps (api on 3001, web on 3000, admin on 3002, workers in background)
- ✓ `curl localhost:3001/health` returns documented JSON shape
- ✓ `curl localhost:3001/health/liveness` returns `{ status: "ok" }`
- ✓ All CI gates green locally (lint, type, test, build, format)
- ✓ Conventional commit format enforced (commitlint blocks non-conformant)
- ✓ Pre-commit secret scan fires on every commit (gitleaks 8.30.1)
- ✓ Lockfile integrity confirmed (`pnpm install --frozen-lockfile` clean)

### Known limitations (intentional, deferred to later phases)

- No DB schema yet (Phase 1.1)
- No auth yet (Phase 1.2)
- No outbox / sagas (Phase 1.3)
- No rules engine (Phase 1.4)
- No documents / notifications wiring (Phase 1.5)
- No production deploy pipeline (Phase 1.6)
- No mobile app (Phase 8)

### Push instructions for the user

```bash
cd /Users/pj/Documents/hrms
ssh -T git@github.com           # confirm SSH auth (should print "Hi pankaj1989!")
git push -u origin main
git push origin --tags
```

After push, watch CI: https://github.com/pankaj1989/HRMS/actions

### Next plan

`docs/superpowers/plans/<date>-p1.1-database-foundations.md` — Drizzle setup, RLS demo, audit hash chain, bitemporal demo table, anonymized seeding pipeline skeleton.
