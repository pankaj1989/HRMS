{
  "name": "@hrms/domain",
  "version": "0.0.0",
  "private": true,
  "description": "HRMS domain package — skeleton, fleshed out in later phases",
  "license": "UNLICENSED",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --max-warnings 0",
    "type": "tsc --noEmit",
    "test": "echo 'tests added in dedicated phase' && exit 0",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@hrms/config": "workspace:*",
    "typescript": "5.6.2"
  }
}
